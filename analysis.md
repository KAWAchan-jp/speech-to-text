# Speech-to-Text Webcam Overlay 解析レポート

このアプリケーションは、Webブラウザ上でマイクから拾った音声をリアルタイムで文字起こしし、Webカメラの映像に字幕として合成するツールです。OBSなどの配信ソフトに取り込んで使うことを想定して作られています。

## 1. 全体像と仕組み
このツールは主に以下の3つの要素を組み合わせて動いています。

* **音声認識 (Web Speech API)**: Chromeなどのブラウザに標準搭載されている無料の音声認識機能を呼び出して文字起こしをしています。
* **カメラ映像 (WebRTC)**: 接続されているWebカメラの映像をブラウザ上で取得して背景に敷いています (`navigator.mediaDevices.getUserMedia`)。
* **字幕合成 (HTML/CSS)**: 映像の上に、音声認識で取得したテキストを重ね合わせて表示しています。

## 2. ファイルの構成と役割

* **`index.html`** (画面の骨組み)
  アプリの画面構成（カメラ領域、字幕表示領域、設定パネル、ログ領域など）が記述されています。
* **`main.js`** (アプリの脳・ロジック)
  「カメラ映像を取得する」「マイク音声を拾って文字にする」「文字を画面に反映する」「ユーザーの設定を保存する」といった実際の動作プログラムがすべてここに書かれています。
* **`style.css`** (画面のデザイン)
  文字のフォント、大きさ、色、背景の透明度、各要素の配置位置などを細かく指定しています。
* **`kuromoji/` フォルダ** (ひらがな変換用ライブラリ)
  日本語の形態素解析エンジンです。漢字で認識された文字に読み仮名を振り、「すべてひらがなで表示する」機能のために使われています。

## 3. 実装されている主な機能

1. **リアルタイムな字幕の反映**
   喋っている途中の文字（未確定）と、喋り終わった文字（確定）を同時に表示して、レスポンスよく字幕を出す仕組みがあります。
2. **字幕の細かなカスタマイズ**
   フォント、大きさ、透明度、位置、フチ取り、影、行間、字間、文字色、自動消去されるまでの時間など、配信で見やすくするための設定が網羅されています。
3. **設定の自動保存 (Local Storage)**
   ブラウザのローカルストレージという機能を使って、スライダーで調整した色や大きさの設定が自動的にブラウザ内に保存されます。次回開いたときも同じ設定が引き継がれます。
4. **翻訳機能 / ひらがな化機能**
   Google翻訳ウィジェットを読み込んで外国語に翻訳したり、「kuromoji」を使ってすべての字幕をひらがなにする機能（`resultToHiragana`）が実装されています。
5. **認識結果のログとダウンロード**
   喋った内容を履歴としてテキストボックスに残し、後からタイムスタンプ付きのテキストファイル（`.txt`）としてダウンロードする機能があります。
6. **グリーンバック（単色背景）機能**
   配信ソフト等でクロマキー合成（背景を透過）しやすいように、背景を黄緑色（#00FF00）にする機能が付いています。

## 4. カスタマイズする際のポイント

ご自身で使いやすく改修したい場合は、以下の点を中心に編集すると良いでしょう。

* **デフォルトの初期設定を変えたい**
  `index.html` の `<input ... value="68">` などのタグを探し、`value` や `checked` の値を変更することで、最初から自分好みの設定で起動させることができます。
* **見た目や配置を劇的に変えたい**
  `style.css` を編集します。たとえば字幕の領域 `.text_area` の `bottom` や `padding` などを変更することで、字幕が出る位置を自由に変更できます。
* **使わない設定項目（スライダーや翻訳など）を消してスッキリさせたい**
  `index.html` の中にある `<div class="control_form ...">` 等の不要なブロックを削除するか、CSSで `display: none;` にすることで、必要な設定だけが並んだシンプルな画面にできます。
* **文字を「自動消去」するまでの時間を変えたい**
  `main.js` 内の `var textUpdateTimeoutSecond = 30;` を変更するか、`index.html` の `<select id="select_autoclear_text">` に自分がよく使う秒数を追加することで使い勝手が良くなります。
